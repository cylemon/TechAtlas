[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
asyncio_mode = auto
markers =
    live: 标记需要真实网络与真实 API Key 的集成测试，可用 -m "not live" 跳过